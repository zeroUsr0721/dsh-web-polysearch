# All @deepseek-ai peer dependencies are provided by the host profile at
# runtime; do not auto-install or try to resolve their peer closures here.
autoInstallPeers=false
strict-peer-dependencies=false